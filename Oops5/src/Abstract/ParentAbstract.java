package Abstract;

public abstract class ParentAbstract {
    // Abstract methods-- They are the just empty methods defined in
// parent classes, so they can be used in child classes.
//It must be overridden
//If any method is declared as abstract, then the class should
// also be declared as abstract
   abstract void career(String name);

}
